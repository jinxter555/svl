SHELL := /bin/bash
CC=g++ -std=c++17 -g
# Define temporary directory
TMPDIR = /dev/shm
ast_o := $(wildcard ../*.o)

all: t_list_01 

t_list_01: t_list_01.o ast_o
	$(CC)  $^ $(ast_o) $(core_o) -o $@

clean:
	rm -f all out *.o *.gch *.out
