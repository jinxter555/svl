module Kernel 
  def main (x y) 
    var forever (count 0)
    = t1  "t1 robot"
    = forever true

     print( fib 2)

  end.def

  def fib(n) 
    if[n == 0]
      return 0
    end.if

    print "n:" n "\n"
    [nv = n - 1]
    = v (fib nv )
    return v
  end.def

end.module
