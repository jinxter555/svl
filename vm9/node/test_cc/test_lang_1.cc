#include "../lang.hh"
#include <iostream>

using namespace  std;
  

int main() {
  /*
  Lang lang;
  auto v1 = lang.str_to_atom("hello");
  auto v2 = lang.str_to_atom("world");

  auto status = lang.atom_to_str(v1);
  cout << "status: " << *status.second << "\n";
*/
}