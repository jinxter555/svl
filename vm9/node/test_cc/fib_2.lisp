module Kernel 
  def main (x y) 
    var forever (count 0)
    = t1  "t1 robot"
    = forever true

    print ( fib 2)

  end.def

  def fib(n) 
    if[n <= 1]
      return n
    :else
      # return (+  (fib[n-1]) fib([n-]))
      return 5
    end.if
  end.def

end.module
